# -*- coding: utf-8 -*-
"""web_app_ml_nn

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1j1YKHrYAtT4TVvdPqTS9SWJNncpYIdje
"""

import streamlit as st
import numpy as np
import tensorflow as tf
from tensorflow.keras.datasets import mnist, cifar10
from tensorflow.keras.models import load_model
import matplotlib.pyplot as plt

# โหลดโมเดลที่ฝึกไว้
mnist_model = load_model("./models/mnist_model.h5")
cifar10_model = load_model("./models/cifar10_model.h5")

# โหลดข้อมูลตัวอย่าง MNIST และ CIFAR-10
(_, _), (X_mnist, _) = mnist.load_data()
(_, _), (X_cifar, _) = cifar10.load_data()
X_mnist = X_mnist.astype("float32") / 255.0
X_cifar = X_cifar.astype("float32") / 255.0
X_mnist = X_mnist.reshape(-1, 28, 28, 1)

# สร้างหน้าเว็บ Streamlit
st.title("Machine Learning & Neural Network Web App")

# สร้าง Sidebar เลือกหน้า
page = st.sidebar.selectbox("เลือกหน้า:", ["📊 ข้อมูล" , "📊 ทฤษฎี", "🤖 ทดสอบ MNIST Model", "🖼️ ทดสอบ CIFAR-10 Model"])

if page == "📊 ข้อมูล":
    st.subheader("🔍 ข้อมูล MNIST")
    st.write("MNIST เป็นชุดข้อมูลของตัวเลขเขียนด้วยลายมือที่ใช้กันอย่างแพร่หลายในการฝึกและทดสอบโมเดลการเรียนรู้ของเครื่อง โดยมีลักษณะดังนี้:")
    st.write("- **ขนาดข้อมูล**: 28x28 ")
    st.write("- **จำนวนตัวอย่าง**: 60,000 ภาพสำหรับฝึก และ 10,000 ภาพสำหรับทดสอบ")
    st.write("- **คลาสของข้อมูล**: ตัวเลข 0-9 (รวมทั้งหมด 10 คลาส)")
    st.write("- **รูปแบบไฟล์**: ภาพขาวดำ (Grayscale)")
    st.write("- **ตัวอย่างการใช้งาน**: ใช้ในงานจำแนกภาพด้วยโมเดลที่มีโครงสร้างง่าย เช่น Logistic Regression, KNN, SVM และ Neural Networks")
    st.write("- **ที่มาของข้อมูล**: [MNIST Database](http://yann.lecun.com/exdb/mnist/)")

    st.subheader("🌍 ข้อมูล CIFAR-10")
    st.write("CIFAR-10 เป็นชุดข้อมูลภาพสีที่ใช้สำหรับจำแนกประเภทของวัตถุในภาพ โดยมีรายละเอียดดังนี้:")
    st.write("- **ขนาดข้อมูล**: 32x32 พิกเซล")
    st.write("- **จำนวนตัวอย่าง**: 50,000 ภาพสำหรับฝึก และ 10,000 ภาพสำหรับทดสอบ")
    st.write("- **คลาสของข้อมูล**: 10 หมวดหมู่ ได้แก่ เครื่องบิน, รถยนต์, นก, แมว, กวาง, สุนัข, กบ, ม้า, เรือ และ รถบรรทุก")
    st.write("- **รูปแบบไฟล์**: ภาพสี (RGB)")
    st.write("- **ตัวอย่างการใช้งาน**: ใช้ในการพัฒนาโมเดล CNN สำหรับจำแนกวัตถุในภาพ")
    st.write("- **ที่มาของข้อมูล**: [CIFAR-10 Dataset](https://www.cs.toronto.edu/~kriz/cifar.html)")
    
elif page == "📊 ทฤษฎี":
    st.subheader("📌 การจำแนกภาพ MNIST")
    st.write("สำหรับ MNIST สามารถใช้หลายอัลกอริทึมในการจำแนกข้อมูล เช่น:")
    st.write("1. **K-Nearest Neighbors (KNN)**: หาค่าที่ใกล้เคียงที่สุดเพื่อจำแนกตัวเลข")
    st.write("2. **Support Vector Machine (SVM)**: ใช้เส้นแบ่งข้อมูลเพื่อจำแนกหมวดหมู่")
    st.write("3. **Random Forest**: ใช้การเรียนรู้ของต้นไม้ตัดสินใจ (Decision Tree)")
    st.write("4. **Convolutional Neural Networks (CNN)**: ใช้โครงข่ายประสาทเทียมเชิงลึกเพื่อเพิ่มประสิทธิภาพการจำแนกภาพ")

    st.subheader("🔥 การจำแนกภาพ CIFAR-10")
    st.write("เนื่องจาก CIFAR-10 เป็นชุดข้อมูลที่มีความซับซ้อนมากกว่าข้อมูลตัวเลขของ MNIST อัลกอริทึมที่ใช้จะเป็นแบบ Deep Learning เช่น:")
    st.write("1. **Convolutional Neural Networks (CNN)**: ใช้เครือข่ายประสาทเทียมที่มีการใช้ Convolutional Layers เพื่อดึงลักษณะเด่นของภาพ")
    st.write("2. **ResNet (Residual Network)**: โมเดลเครือข่ายประสาทที่ลึกขึ้นและมีประสิทธิภาพดีขึ้นโดยใช้ Residual Connections")
    st.write("3. **VGGNet**: โมเดลที่มีโครงสร้างลึกขึ้นที่นิยมใช้กับการจำแนกภาพ")

elif page == "🤖 ทดสอบ MNIST Model":
    st.header("🤖 ทดสอบ MNIST Model")
    index = st.slider("เลือกภาพ (0-9999):", 0, 9999, 0)
    image = X_mnist[index]
    pred = mnist_model.predict(image.reshape(1, 28, 28, 1))
    label = np.argmax(pred)
    st.image(image.reshape(28, 28), caption=f"Prediction: {label}", width=150)

elif page == "🖼️ ทดสอบ CIFAR-10 Model":
    st.header("🖼️ ทดสอบ CIFAR-10 Model")
    class_names = ["airplane", "automobile", "bird", "cat", "deer", "dog", "frog", "horse", "ship", "truck"]
    index = st.slider("เลือกภาพ (0-9999):", 0, 9999, 0)
    image = X_cifar[index]
    pred = cifar10_model.predict(image.reshape(1, 32, 32, 3))
    label = class_names[np.argmax(pred)]
    st.image(image, caption=f"Prediction: {label}", width=150)
